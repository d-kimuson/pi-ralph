## Review

- Correct: npm trusted publishing / provenance の前提は満たしています。`.github/workflows/release.yaml:12-14` の publish job permissions は `contents: read` と `id-token: write` のみで、`pnpm publish --provenance` が必要とする GitHub Actions OIDC トークン権限を明示しています。publish は tag push 時の GitHub Actions 上でのみ実行され、ローカル CI から npm token で publish する構成ではありません（`.github/workflows/release.yaml:3-6`, `.github/workflows/release.yaml:32-33`）。
- Correct: npm token / secret の露出は見当たりません。publish step は `NODE_AUTH_TOKEN` や npm token secret を渡しておらず、trusted publishing(OIDC)前提です（`.github/workflows/release.yaml:32-33`）。release notes 側は GitHub Release 作成用に `GITHUB_TOKEN` のみを `changelogithub` に渡しています（`.github/workflows/release.yaml:50-53`）。
- Correct: workflow permissions は概ね最小です。publish job は npm provenance に必要な `id-token: write` と checkout 用の `contents: read` のみ（`.github/workflows/release.yaml:12-14`）。release-notes job は draft release 作成のため `contents: write` のみ（`.github/workflows/release.yaml:39-40`）。
- Correct: checkout / setup-node の pin は remote-agent と一致しています。pi-ralph は `actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd` を release workflow の両 job で使用（`.github/workflows/release.yaml:16-17`, `.github/workflows/release.yaml:42-43`）、remote-agent も同一 hash（`../remote-agent/.github/workflows/release.yaml:16-17`, `../remote-agent/.github/workflows/release.yaml:42-43`）。composite action 内の `actions/setup-node@8f152de45cc393bb48ce5d89d36b731f54556e65` も remote-agent と一致（`.github/actions/setup-node/action.yml:6-8`, `../remote-agent/.github/actions/setup-node/action.yaml:6-8`）。
- Correct: `publishConfig` は危険な設定を追加していません。`package.json:45-47` は `access: public` のみで、registry の差し替えや lifecycle script の追加などはありません。
- Note: Git signing check は現在のユーザー環境には合っています。実際の repo/global config は `gpg.format=ssh`, `commit.gpgsign=true`, `tag.gpgsign=true` でした。`scripts/release.ts:107-117` も同じ条件を要求し、`git commit -S` と `git tag -s` を実行します（`scripts/release.ts:276-278`）。
- Note: ただし Git signing check は SSH signing 専用です。GPG signing を使っているユーザー（`gpg.format` 未設定または `openpgp`）は、`commit.gpgsign=true` と `tag.gpgsign=true` でも `scripts/release.ts:111-117` で失敗します。これはセキュリティ上の脆弱性ではありませんが、GPG signing もサポートしたい要件なら運用上のブロッカーになります。現要件が「このユーザーの SSH signing setup を強制」なら妥当です。
- Note: `plan.md` と `progress.md` は指定パスに存在しませんでした（read 時に ENOENT）。そのため、レビューは実ファイルと remote-agent 比較に基づいています。

結論: npm publishing setup は trusted publishing / provenance として安全かつ正しい構成です。重大な security issue は見つかりませんでした。運用上の注意点は、release script が GPG signing ではなく SSH signing のみを許可する点です。
